{{>layout/header}}

<h1>게시글 수정</h1>

<div class="col-md-12">
    <div class="col-md-4">
        <form>
            <div class="form-group">
                <label for="title">글 번호</label>
<!--                <textarea type="text" class="form-control" id="id" value="{{post.id}}" readonly></textarea>-->
                <p>{{post.id}}</p>
            </div>
            <div class="form-group">
                <label for="title">제목</label>
                <p>{{post.title}}</p>
            </div>
            <div class="form-group">
                <label for="author"> 작성자 </label>
<!--                <textarea type="text" class="form-control" id="id" value="{{post.author}}" readonly></textarea>-->
                <p>{{post.author}}</p>
            </div>
            <div class="form-group">
                <label for="content"> 내용 </label>
<!--                <textarea class="form-control" id="content">{{post.content}}</textarea>-->
                <p>{{post.content}}</p>
            </div>
        </form>
        <a href="/" role="button" class="btn btn-secondary">취소</a>
<!--        <button type="button" class="btn btn-primary" id="btn-update">수정 완료</button>-->
        <a href="/posts/update/{{post.id}}" role="button" class="btn btn-secondary">수정</a>
<!--        <button type="button" class="btn btn-danger" id="btn-delete">삭제</button>-->
    </div>
</div>

{{>layout/footer}}